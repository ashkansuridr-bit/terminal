<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
        Normal permission, no runtime prompt. Read only to answer "is this connection
        metered?" for the Wi-Fi-only transfer setting; the app never inspects traffic.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".TerminalApp"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden|navigation|uiMode|density|fontScale|locale|layoutDirection">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          dataSync carries a ~6h/24h rolling execution cap starting with Android 15,
          which would cut off a long-lived interactive SSH session left backgrounded
          overnight. This service keeps a user-initiated terminal connection alive,
          not a data transfer job, so it declares specialUse with an explicit reason
          instead of misusing dataSync for the notification-permission side effect.
        -->
        <service
            android:name=".service.SshForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps user-initiated SSH terminal sessions connected while the app is backgrounded" />
        </service>

        <!--
            Exposes live SSH sessions in Android's file picker so any app can open and
            save files straight off the server.

            exported=true is required — the system's DocumentsUI is a different process —
            but the gate is android.permission.MANAGE_DOCUMENTS, a signature-level
            permission only the platform holds. No third-party app can bind to it, and
            only sessions the user has already connected appear as roots.
        -->
        <provider
            android:name=".sftp.SftpDocumentsProvider"
            android:authorities="app.terminalssh.secure.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <!--
            Hands a staged copy of a remote file to another app for "open with" / "share".
            Not exported: access is only ever the one-shot read grant attached to the
            intent, which Android revokes when the receiving activity finishes.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.shared"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/shared_files" />
        </provider>
</application>
</manifest>
